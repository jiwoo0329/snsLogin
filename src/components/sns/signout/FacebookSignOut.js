export default function FacebookSignOut() {
    return (
        <>
            {/* <button
                type="button"
                onClick={() => {
                    FB.logout((res) => {
                        console.log(res);
                    });
                }}
            >
                페이스북 로그아웃
            </button> */}
        </>
    );
}
